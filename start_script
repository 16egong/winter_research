if [ $# -lt 2 ]; then
    echo "Invalid Usage. Please provide a container name, port"
    echo -e "\winter <container name> <jupyter port>"
    exit 1
fi

docker run --rm -p $2:8888 -it --name $1 -e JUPPORT=8080 -v "$(pwd):/code/" alion_eg
